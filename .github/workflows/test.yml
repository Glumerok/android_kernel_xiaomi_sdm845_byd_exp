name: Test Workflow
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
    - name: Test
      run: |
        echo "Test started!"
        echo "If you see this, GitHub Actions work"
        uname -a
        pwd
        ls -la
